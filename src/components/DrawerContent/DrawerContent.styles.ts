import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerDrawer: {
    flexDirection: "row",
    padding: 20,
  },
  headerTitulo: {
    marginTop: 10,
    marginLeft: 20,
    fontWeight: "bold",
  },
  drawerStyle: {
    paddingLeft: 10,
  },
});
export default styles;
